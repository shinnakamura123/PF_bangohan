<div class ="container p-5">
  <div class ="row">
    <div class ="col-md-11 offset-md-1">
      <div class="row ml-3">
        <h3 class="font-weight-bold">いいねしたレシピ一覧</h3>
        <p class="p-2">(全<%= @favorites.total_count %>件）<p>
      </div>
      <div><%= link_to 'マイリスト一覧', recipe_lists_path(current_user), class: 'btn btn-success' %></div>
      <div class="row">
        <% @favorites.each do |recipe| %>
          <div class="card border-light m-2">
            <%= link_to recipe_path(recipe) do %>
              <%= image_tag recipe.get_image(300, 300) ,class: "shadow mb-5 bg-body round" %>
            <% end %>
            <div class="card-body">
              <h5 class="card-title">レシピ名：<%= recipe.recipe_name %></h5>
              <p class="card-text"><%= recipe.point %></p>
              <p class="card-text">制作者：<%= recipe.user.name %></p>
              <% if recipe.listed_by?(current_user) %>
                <p class="card-text"><%= link_to 'マイリストから外す', recipe_list_path(recipe.id), method: :delete, class: 'btn btn-danger' %></p>
              <% else %>
                <p class="card-text"><%= link_to 'マイリストに追加する', recipe_lists_path(recipe.id), method: :post, class: 'btn btn-info' %></p>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="offset-md-5 mt-5">
      <%= paginate @favorites, theme: 'twitter-bootstrap-4' %>
    </div>
  </div>
</div>